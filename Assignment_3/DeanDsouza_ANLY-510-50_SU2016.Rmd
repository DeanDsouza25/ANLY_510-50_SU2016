---
title: "DeanDsouza_ANLY-510-50_SU2016"
author: "Dean D'souza"
date: "`sys.Date()`"
output: word_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## Regression on Data Set 1: Segmentation Data

```{r,echo=FALSE,warning=FALSE,message=FALSE}
library(caret)
data("segmentationData")
head(segmentationData)
dataeu<-segmentationData
remove(segmentationData)
```

#### The Analysis
  
  As the dataset consists of more than two numeric rows, we go forward with determining the correlation among variables.
  
```{r, warning=FALSE,message=FALSE}

```


## Regression on Data Set 2 : Sacramento

```{r,echo=FALSE,message=FALSE,warning=FALSE}
data("Sacramento")
head(Sacramento)
datasac<-Sacramento
remove(Sacramento)
```

#### The Analysis
  
  As the data set consists of more than two numeric rows, we go forward with determining the correlation among variables.
  
```{r,warning=FALSE,message=FALSE}

```
